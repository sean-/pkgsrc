# $NetBSD$
#

# _pkgformat-check-vulnerable:
#	Checks for known vulnerabilities in the package if a vulnerability
#	file exists.
#
_pkgformat-check-vulnerable: .PHONY
	# XXX: unimplemented, would need support in audit-packages
	${RUN}${DO_NADA}
